# Generated by scripts/migrate_codex.py from .claude/settings*.json.
#
# This generator only performs formal Bash permission migration.
# Migrate Claude skills, agents, docs, and prose with the migrate-claude skill.
# Source summary: 83 shell allow rule(s), 2 shell deny rule(s).
# Non-shell Claude permissions are handled by Codex tools/sandbox instead:
# Bash, Edit, Edit(/.claude/agents/**), Edit(/.claude/skills/**), Edit(memory/**), Read, WebFetch, WebSearch, Write, Write(memory/**)

# Source: Bash(sudo:*)
prefix_rule(
    pattern = [
        ["sudo"],
    ],
    decision = "forbidden",
    justification = "Migrated from Claude forbidden permission.",
    match = ["sudo"],
)

# Source: Bash(rm -rf /:*)
prefix_rule(
    pattern = ["rm", "-rf", "/"],
    decision = "forbidden",
    justification = "Migrated from Claude forbidden permission.",
    match = ["rm -rf /"],
)

# Source: Bash(./scripts/gen_proto.sh:*), Bash(awk:*), Bash(cat:*), Bash(cd:*), Bash(command *), Bash(csharpier:*), Bash(echo:*), Bash(file:*), Bash(find *), Bash(gh:*), Bash(grep:*), Bash(head:*), Bash(just:*), Bash(ls:*), Bash(protoc --version:*), Bash(protoc:*), Bash(rg:*), Bash(scripts/gen_proto.sh:*), Bash(shellcheck *), Bash(shfmt *), Bash(stat:*), Bash(strings *), Bash(tail:*), Bash(tree:*), Bash(unzip *), Bash(uvx *), Bash(wc:*)
prefix_rule(
    pattern = [
        [
            "./scripts/gen_proto.sh",
            "awk",
            "cat",
            "cd",
            "command",
            "csharpier",
            "echo",
            "file",
            "find",
            "gh",
            "grep",
            "head",
            "just",
            "ls",
            "protoc",
            "rg",
            "scripts/gen_proto.sh",
            "shellcheck",
            "shfmt",
            "stat",
            "strings",
            "tail",
            "tree",
            "unzip",
            "uvx",
            "wc",
        ],
    ],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["./scripts/gen_proto.sh"],
)

# Source: Bash(bash scripts/*)
prefix_rule(
    pattern = [
        "bash",
        [
            "scripts/container-init.sh",
            "scripts/decompile.sh",
            "scripts/gen_proto.sh",
            "scripts/host_agent.py",
            "scripts/lib.sh",
            "scripts/migrate_codex.py",
            "scripts/renderer-prebuilt-hash.sh",
            "scripts/requirements.txt",
            "scripts/resize_icon.py",
            "scripts/resonite_cli.py",
        ],
    ],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["bash scripts/container-init.sh"],
)

# Source: Bash(docker compose *)
prefix_rule(
    pattern = [
        "docker",
        ["compose"],
    ],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["docker compose"],
)

# Source: Bash(dotnet --info:*), Bash(dotnet --version:*), Bash(dotnet add:*), Bash(dotnet build:*), Bash(dotnet csharpier:*), Bash(dotnet format:*), Bash(dotnet ilspycmd *), Bash(dotnet new:*), Bash(dotnet remove:*), Bash(dotnet restore:*), Bash(dotnet sln:*), Bash(dotnet test:*), Bash(dotnet tool *), Bash(dotnet tool install:*), Bash(dotnet tool list:*), Bash(dotnet tool update:*)
prefix_rule(
    pattern = [
        "dotnet",
        [
            "--info",
            "--version",
            "add",
            "build",
            "csharpier",
            "format",
            "ilspycmd",
            "new",
            "remove",
            "restore",
            "sln",
            "test",
            "tool",
        ],
    ],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["dotnet --info"],
)

# Source: Bash(git add:*), Bash(git blame:*), Bash(git branch:*), Bash(git check-ignore *), Bash(git checkout:*), Bash(git commit:*), Bash(git diff:*), Bash(git fetch *), Bash(git log:*), Bash(git mv *), Bash(git pull *), Bash(git push:*), Bash(git remote:*), Bash(git restore *), Bash(git rev-parse:*), Bash(git rm *), Bash(git show:*), Bash(git stash:*), Bash(git status:*), Bash(git switch *), Bash(git switch:*), Bash(git tag:*)
prefix_rule(
    pattern = [
        "git",
        [
            "add",
            "blame",
            "branch",
            "check-ignore",
            "checkout",
            "commit",
            "diff",
            "fetch",
            "log",
            "mv",
            "pull",
            "push",
            "remote",
            "restore",
            "rev-parse",
            "rm",
            "show",
            "stash",
            "status",
            "switch",
            "tag",
        ],
    ],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["git add"],
)

# Source: Bash(pre-commit run *)
prefix_rule(
    pattern = [
        "pre-commit",
        ["run"],
    ],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["pre-commit run"],
)

# Source: Bash(uv add:*)
prefix_rule(
    pattern = ["uv", "add"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv add"],
)

# Source: Bash(uv build *)
prefix_rule(
    pattern = ["uv", "build"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv build"],
)

# Source: Bash(uv lock:*)
prefix_rule(
    pattern = ["uv", "lock"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv lock"],
)

# Source: Bash(uv pip show:*)
prefix_rule(
    pattern = ["uv", "pip", "show"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv pip show"],
)

# Source: Bash(uv python:*)
prefix_rule(
    pattern = ["uv", "python"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv python"],
)

# Source: Bash(uv python3:*)
prefix_rule(
    pattern = ["uv", "python3"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv python3"],
)

# Source: Bash(uv run pre-commit:*)
prefix_rule(
    pattern = ["uv", "run", "pre-commit"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv run pre-commit"],
)

# Source: Bash(uv run pyright:*)
prefix_rule(
    pattern = ["uv", "run", "pyright"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv run pyright"],
)

# Source: Bash(uv run pytest:*)
prefix_rule(
    pattern = ["uv", "run", "pytest"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv run pytest"],
)

# Source: Bash(uv run python:*)
prefix_rule(
    pattern = ["uv", "run", "python"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv run python"],
)

# Source: Bash(uv run python3:*)
prefix_rule(
    pattern = ["uv", "run", "python3"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv run python3"],
)

# Source: Bash(uv run resoio:*)
prefix_rule(
    pattern = ["uv", "run", "resoio"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv run resoio"],
)

# Source: Bash(uv run ruff:*)
prefix_rule(
    pattern = ["uv", "run", "ruff"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv run ruff"],
)

# Source: Bash(uv sync:*)
prefix_rule(
    pattern = ["uv", "sync"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv sync"],
)

# Source: Bash(uv venv:*)
prefix_rule(
    pattern = ["uv", "venv"],
    decision = "allow",
    justification = "Migrated from Claude allow permission.",
    match = ["uv venv"],
)
